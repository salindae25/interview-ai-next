import { HStack, VStack } from "@chakra-ui/layout"

export const NavigationBar = () => {
    return (
        <HStack bg="white" height="70px" width="full">
            navigation
        </HStack>
    )
}